# Bug-finding memories

Open or rejected PR entries only (one line each: location/root cause, PR URL, status, date).

- `lib/screens/home_screen.dart` Your Car: StreamBuilder `initialData` only on first subscribe; empty realtime emission leaves chips blank despite `myCars()` seed — https://github.com/Liyohhh/Waby/pull/new/fix/home-cars-empty-stream-fallback — status: open (branch pushed; PR create URL — `gh` not authenticated) — 2026-07-30
